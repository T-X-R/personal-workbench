# Personal Workbench Domain Context

## Glossary

- **工作台（Workbench）**：承载个人工作入口、已启用能力、全局命令和设置的宿主产品。工作台本身不提供具体业务产出。
- **能力（Capability）**：可被单独安装、启用、停用和卸载的功能单元，例如周报、表格或日记。
- **能力包（Capability Package）**：能力的可分发单元，包含能力的身份信息、版本、入口、可见界面和所需权限。
- **能力注册表（Capability Registry）**：工作台维护的能力目录，记录能力包是否已安装、是否启用、版本和兼容性状态。
- **安装（Install）**：将能力包纳入当前工作台并完成兼容性检查；安装不等于启用。
- **启用（Enable）**：允许已安装能力出现在工作台入口并响应其命令。
- **停用（Disable）**：保留能力包及其数据，但暂时不让能力参与工作台运行。
- **卸载（Uninstall）**：移除能力包；能力数据是否删除必须由用户单独确认。
- **能力宿主（Capability Host）**：工作台向能力提供的受控运行环境。能力通过宿主获得存储、命令、活动和可选智能代理能力，不直接拥有工作台内部实现。
- **活动事件（Activity Event）**：工作台或已授权能力产生的、可被用户查看或后续智能代理汇总的事实记录。
- **智能代理能力（Agent Capability）**：一种可选能力，读取已授权的活动事件并执行可解释、可撤销的生成或整理动作。
- **AI Provider**：工作台用于执行模型调用的统一提供方。它可以是本机 Codex CLI 登录会话、API key 或兼容 Responses 协议的自定义端点。
- **凭据托管（Credential Broker）**：平台级的凭据解析与调用代理。它向平台提供一次性配置后的可用 Provider，不向能力包返回原始 token 或 API key。
- **Agent Host**：平台向能力提供的统一 Agent 调用接口。能力只提交任务、输入引用和工具范围，不负责选择凭据或拼接供应商请求。
- **Codex CLI Adapter**：将 Agent Host 请求转交给本机 Codex CLI 的适配器，复用 Codex 自己的配置、登录状态和沙箱策略。
- **工作区（Workspace）**：能力和活动事件共同使用的个人上下文容器。第一版只有一个默认工作区，但概念上不把能力绑定到具体页面。

## Invariants

- 工作台没有任何业务能力时仍然可启动、可导航和可设置。
- 未安装或已停用的能力不会出现在默认工作入口，也不会处理命令或后台任务。
- 能力只能通过能力宿主访问平台资源；能力不能依赖工作台内部页面结构。
- 安装、启用、停用和卸载是四个不同状态变化；卸载能力包不自动删除用户数据。
- 智能代理只读取用户明确授权的数据源，并且每次生成结果都能追溯到输入活动事件。
- AI 凭据只在平台托管层使用；能力包永远不能读取原始 token、API key、Codex 登录文件或环境变量。
- 订阅登录和 API key 是两类不同的凭据来源；平台显示当前来源，不把订阅 token 当作 API key 使用。
